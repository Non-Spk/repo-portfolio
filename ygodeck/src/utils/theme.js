import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: {
      100: "#EEEEEE",
      200: "#76ABAE",
      300: "#31363F",
      400: "#222831"
    }
  }
});

export default theme;
